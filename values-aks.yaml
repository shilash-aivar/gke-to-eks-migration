# Velero on AKS (source) — backs up to AWS S3 for EKS restore
image:
  repository: velero/velero
  tag: v1.18.0
  pullPolicy: IfNotPresent

initContainers:
  - name: velero-plugin-for-aws
    image: velero/velero-plugin-for-aws:v1.11.0
    imagePullPolicy: IfNotPresent
    volumeMounts:
      - mountPath: /target
        name: plugins

configuration:
  backupStorageLocation:
    - name: default
      provider: aws
      bucket: migration-bucket-aks-eks-velero-poc
      default: true
      config:
        region: us-east-1

  volumeSnapshotLocation:
    - name: default
      provider: aws
      config:
        region: us-east-1

  defaultVolumesToFsBackup: true
  uploaderType: kopia

# Create secret before helm install (never commit keys):
#   kubectl -n velero create secret generic velero-credentials \
#     --from-file=cloud=aws-credentials
# Or: AWS_CREDS_FILE=./aws-credentials ./scripts/setup-aks-poc.sh
credentials:
  useSecret: true
  name: velero-credentials
  secretContents:
    cloud: |
      [default]
      aws_access_key_id=REPLACE_ME
      aws_secret_access_key=REPLACE_ME

deployNodeAgent: true

nodeAgent:
  podVolumePath: /var/lib/kubelet/pods
  privileged: false
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

metrics:
  enabled: false
